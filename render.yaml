services:
  - type: web
    name: vinopro-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python render_start.py
    autoDeploy: true
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: DEBUG
        value: "0"
